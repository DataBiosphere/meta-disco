"""Package-data home for the canonical LinkML classification schema.

`classification.yaml` is the single source of truth for the controlled
vocabulary. It lives with the runtime that reads it (`schema_vocab.py`); the
`schema/` project is the LinkML tooling that maintains and validates it.
"""
